const attributes = ( { name="", value } ) =>{      
    
        return(
                <h1>
                    name={name}; value={value}
                </h1>
        )
    }
    
    export default attributes;